<div class="col-sm-3 col-md-2 sidebar">
    <ul class="nav nav-sidebar">
        {% for repoId, repoName in repositories %}
        <li><a href="{{ url('repository', {'repository': repoId}) }}">{{ repoName }}</a></li>
        {% endfor %}
    </ul>
</div>